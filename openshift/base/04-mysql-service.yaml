# Unchanged from k8s/mysql-service.yaml. Internal-only, so ClusterIP already
# was the right type.
apiVersion: v1
kind: Service
metadata:
  name: mysql
spec:
  selector:
    app: mysql
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
